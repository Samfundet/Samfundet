= link_to admissions_admin_admission_group_path(@interview_group.admission, @interview_group.group) do
  .samf-button.plain.mb-3
    = t("common.back")

%h1
  = "Rediger Intervjugruppe"
%h2
  = set_and_return_title(@interview_group.group.name + " - " + @interview_group.admission.title)

!= render 'form'
