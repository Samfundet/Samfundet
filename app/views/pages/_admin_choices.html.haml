- if @show_admin
  .admin-choices
    %ul
      - if can? :new, Page
        %li
          = link_to t("pages.new_page"), new_page_path

      - if can? :edit, @page
        %li
          = link_to t("crud.edit"), edit_page_path(@page)

      - if can? :destroy, @page
        %li
          = link_to t("crud.destroy"), @page, data: { confirm: t("pages.confirm_delete") }, method: :delete

      %li
        = link_to t("pages.admin"), admin_pages_path

      %li
        = link_to t("pages.graph"), {controller: :pages, action: :graph}
