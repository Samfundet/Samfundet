.flex-row-mobile-column.flex.p-2.pl-3.pr-3.mb-2{style: "background: #f4f4f4; border-radius: 4px; color: #555;"}
  .text-item.center
    = "#{t("unavailable_interview_time_slots.time_frame")}: #{unavailable_interview_time_slot.start_time.strftime("%H:%M")} -  #{unavailable_interview_time_slot.end_time.strftime("%H:%M")}"
  = link_to edit_admission_unavailable_interview_time_slot_path(@admission, unavailable_interview_time_slot) do
    .samf-button.small.green
      = t("unavailable_interview_time_slots.edit")
  = link_to admission_unavailable_interview_time_slot_path(@admission, unavailable_interview_time_slot), method: :delete, data: {confirm: t("crud.confirm")} do
    .samf-button.small.red.ml-1
      = t("unavailable_interview_time_slots.delete")
