

%h1
  = set_and_return_title t("members.control_panel")
.name
  = @wise_words

.flex.flex-justify-center
  .samf-button.small.plain.m-1
    = link_to page_by_name("intern-faq") do
      = t('navigation.admin.faq')
  .samf-button.small.plain.m-1
    = link_to "www.samfundet4.no/login" do
      = t('navigation.admin.samf4_login')

%br

#applets
  - @applets.each do |applet|
    .applet
      = applet.render
